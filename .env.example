# Base URL of the backend API (see ../backend). Not yet consumed by any
# component — src/utils/data.ts is still the live source of truth for the
# site's static content. src/lib/api.ts is ready to wire up once the
# backend is reachable and appointment/inquiry forms exist on the site.
VITE_API_URL=http://localhost:3000/api/v1
